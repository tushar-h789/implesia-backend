meta {
  name: Update Page
  type: http
  seq: 2
}

patch {
  url: {{PRIVATE_BASE_URL}}/api/v1/admin/portfolio
  body: json
  auth: none
}

body:json {
  {
    "hero_heading": "Production software that delivers.",
    "cta_heading": "Ready to build something remarkable?"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
